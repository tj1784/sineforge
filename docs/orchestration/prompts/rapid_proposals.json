{
  "schema_version": "sineforge.orchestration-prompt-history/v1",
  "artifact_format": "json",
  "artifact_filename": "rapid_proposals.json",
  "status": "historical_reference",
  "source": {
    "user_text": "You are Grok 4.5 High implementing CineForge Storyboard Phase 1 proposal schema, validation, deterministic diff, review, rejection, and atomic apply. No tools; use controller context. Return strict JSON complete-file patches with supplied SHA-256. Own only existing `backend/app/services/ai_orchestration/schemas.py`, `validator.py`; new `backend/app/schemas/proposals.py`; new `proposal_service.py`, `proposal_diff.py`, `proposal_apply.py`, `storyboard_mutations.py`; new `backend/app/api/routes/proposal_review.py`; and new proposal tests.\r\n\r\nUse strict extra-forbid nested Project→Story→Characters/Voices/Chapters→Scenes→Shots contracts with stable client IDs, exact duration, ordering, narration/prompt/continuity/reference checks, normalized forbidden-key scanning, factual provider/model/workflow references, and approved voice preservation. Qwen may be recommended only when voice is required and native speech is explicitly unsupported; unknown is review/no Qwen; native-speech LTX uses no Qwen. Apply stored proposals in one transaction with PostgreSQL row locks, stale base ID+content hash checks, one new immutable draft version, flush-before-pointer, one audit/event, no existing commit-per-entity helpers, no external calls. Do not edit ORM/migrations/router/main.\r\n"
  }
}
